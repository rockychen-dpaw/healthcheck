apiVersion: apps/v1
kind: Deployment
metadata:
  name: healthcheck-deployment
spec:
  template:
    spec:
      containers:
        - name: healthcheck
          envFrom:
            - secretRef:
                name: healthcheck-env-uat
                optional: false
